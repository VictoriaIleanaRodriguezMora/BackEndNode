<%- include("../partials/head"); %>

    <!-- HEADER -->
    <%- include("../partials/navbar_logged"); %>
        <!-- HEADER -->

        <div id="fail_auth">

            <h1>SignUp Error, try it again</h1>
            <p>You are being redirect to SIGNUP</p>

            <button>
                <a href="/auth/login">Go to Login</a>
            </button>

        </div>

        <script>
            setTimeout(() => { window.location.href = "/signup"; }, 3000);
        </script>

        <!-- FOOTER -->
        <%- include("../partials/footer"); %>
            <!-- FOOTER -->