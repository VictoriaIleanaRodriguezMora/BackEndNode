const { generateURL } = require("./GeneratorUrl")
const { percentageCalculator } = require("./percentageCalculator")
const { generateProds } = require("./GeneratorProds")

module.exports = { percentageCalculator, generateURL, generateProds }